val closures = (x:Int) => x+num

var num=10
println("num is [" + num + "]")
println("Plus num in function closures(10) is [" + closures(10) + "]")

println

num=13
println("num is [" + num + "]")
println("Plus num in function closures(10) is [" + closures(10) + "]")

